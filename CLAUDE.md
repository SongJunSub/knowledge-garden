# knowledge-garden — 프로젝트 규칙

읽은 좋은 글을 요약하고 내 생각을 덧붙이는 디지털 가든. 단순 링크 모음이 아니라 **왜 좋았고 어디에 쓸 수 있을지**까지 남긴다.

## 언어

사용자·레포 모두 한국어 기반. **이 레포 관련 모든 사용자 대면 텍스트는 한글로 쓴다** — 대화 답변뿐 아니라 PR 제목·본문, 커밋 메시지의 서술 부분, PushNotification(스케줄 루틴 알림) 등 전부 포함. 노트 프론트매터 필드명·코드·영문 고유명사(제목·인용 등)는 예외.

## 자율 워크플로우 — "정리해줘" = 커밋·푸시까지 전부 자동

사용자가 글 URL(주로 GeekNews `news.hada.io/topic?id=…`, 그 외 블로그·유튜브·GitHub 등)을 주며 **"정리해줘" / "각각 정리해줘"** 라고 하면, 아래 파이프라인을 **끝까지 자율로** 수행한다. **중간에 커밋 여부·방식을 묻지 않는다. 최종적으로 `origin/main`에 푸시까지 완료한다.** (이 지시가 전역 규칙 "커밋은 요청 시에만 / default 브랜치면 브랜치 먼저"를 이 레포에 한해 오버라이드한다. 이 개인 가든은 히스토리 전체가 main 직접 커밋이며, 별도 브랜치·PR 없이 main에 커밋 후 push 하는 것이 곧 "머지"다.)

파이프라인:
1. **원문 확보** — GeekNews 토픽 페이지를 WebFetch 하고, 필요하면 원문(블로그·논문·유튜브)도 별도로 WebFetch 해 세부·인용·수치를 보강한다. 원문이 봇 차단 등으로 막히면 그 사실을 요약에 "출처 한계"로 명시한다.
2. **분류** — 아래 카테고리 중 하나를 고른다.
3. **요약 노트 작성** — 아래 형식으로 `카테고리/YYYY-MM-DD-슬러그.md` 생성(날짜 = 정리일, 슬러그 = 영문 kebab-case).
4. **README 인덱스 갱신** — `## 인덱스 (최신순)`의 해당 월 섹션 **최상단**에 밀도 높은 한 줄 항목 추가.
5. **링크 검증** — 새 노트의 in-file 위키링크(전체 파일명)가 전부 실재 파일로 resolve 되는지 확인(dead link 0).
6. **커밋 → 푸시** (아래 규칙).

## 커밋 규칙 (하네스)

- **요약 1개 = 커밋 1개.** 그 커밋에는 **해당 `.md` 파일 하나만** 담는다(README·다른 파일 섞지 않음).
- **README 인덱스는 별도 배치 커밋**으로 한 번에 반영한다(여러 글을 한 번에 정리하면 README 한 커밋으로 묶음).
- 커밋 메시지 형식(레포 관례):
  - 요약: `docs: add '제목(저자)' summary — 핵심 통찰 한 줄`
  - README: `docs: update README index with N articles (YYYY-MM-DD[~DD] batch)`
- 모든 커밋 메시지 끝에 트레일러:
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  ```
- 커밋이 끝나면 **`git push origin main`** 으로 원격까지 반영한다.
- Conventional Commits 를 따르되, 제목 길이는 레포 관례대로 길어도 된다(— 뒤에 핵심 통찰).

## 카테고리 폴더

- `engineering/` — 엔지니어링 원칙·방법론·소프트웨어 철학·개발자 경험
- `architecture/` — 시스템 설계·아키텍처 패턴·분산 시스템
- `backend/` — 백엔드·DB·API·성능
- `frontend/` — 프론트엔드·UX·웹 플랫폼·디자인/브랜딩
- `ai/` — LLM·프롬프트·AI 제품/도구·AI 경제
- `career/` — 커리어·협업·시니어십·성장·조직·비즈니스
- `weekly/` — 주간 다이제스트 큐레이션
- `templates/` — 글 작성 템플릿(`templates/article.md`)

## 자동 스케줄 워크플로우 — Slack 뉴스 다이제스트 (일간, 완전 자동 · PR·머지까지)

매일 스케줄로 트리거되는 별도 루틴. 원래 사용자가 매일 손으로 GeekNews 링크를 옮겨가며 정리하던 작업을 그대로 자동화한 것이라, **품질을 낮추지 않는다** — Slack `#개발-뉴스-dev-news` (https://ondainc.slack.com/archives/C0AJL0096H4) 채널에서 지난 실행 이후 새로 올라온 링크를 모아, 각각을 **위 "정리해줘" 워크플로우와 완전히 동일한 파이프라인·형식·품질 기준**으로 정리한다(라이트 요약 별도 등급 없음). 트리거만 Slack 스케줄이고 나머지는 전부 "정리해줘"와 같다. **끝까지 자율로 수행하고 중간에 커밋·PR·머지 여부를 묻지 않는다.**

파이프라인:
1. **범위 판단** — 레포 루트 `.slack-digest-state.json`의 `last_message_ts`를 "마지막 실행 이후" 기준점으로 삼아 그 이후 새 메시지만 수집한다. 파일이 없으면 최초 실행이므로 최근 24시간만 처리한다. 새 링크가 없으면 상태 파일도 건드리지 않고 짧게 확인만 하고 끝낸다.
2. **원문 확보 → 분류 → 정식 노트 작성 → README 갱신 → 링크 검증** — 위 "자율 워크플로우" 1~5단계를 새로 수집한 링크마다 그대로 수행한다. 노트 위치(카테고리 폴더)·형식(요약 노트 형식)·핵심 전이·CRS 적용점·정직성 기준 전부 "정리해줘"와 동일하게 적용한다.
3. **커밋** — 위 커밋 규칙과 동일(요약 1개 = 커밋 1개, README는 배치로 1개).
4. **상태 파일 갱신** — 그 실행에서 처리한 마지막 메시지의 `last_message_ts`로 `.slack-digest-state.json`을 갱신하고 커밋(README 배치 커밋에 함께 묶는다).
5. **PR 생성 → main 머지까지 자동 완료.** 이 스케줄 루틴의 세션은 지정된 개발 브랜치에서 작업하도록 제약되는 경우가 있다(하네스 지시). 그 경우 main 직접 push 대신: 지정 브랜치로 push → GitHub PR 생성(base: `main`) → **즉시 squash 머지**까지 전부 자율로 수행한다. PR 생성·머지 여부를 사용자에게 묻지 않는다 — 이 파이프라인에서는 "PR 생성 후 머지"가 곧 "정리해줘" 워크플로우의 "main에 push"와 동급이다.
6. **다음 실행 준비** — PR이 머지된 뒤 지정 브랜치는 이미 머지된 히스토리이므로, 다음 실행 시작 시 `origin/main` 기준으로 브랜치를 재설정(`git fetch origin main && git checkout -B <지정 브랜치> origin/main`)하고 이어간다. **이 재설정은 로컬에서 끝나지 않는다** — `git push origin <지정 브랜치> --force-with-lease` 로 원격 브랜치도 같은 커밋으로 맞춰야 stop hook의 "unpushed commit" 경고가 안 뜬다.

## 요약 노트 형식

프론트매터 + 아래 섹션. 실제 톤은 밀도 높고 관점이 분명하게(가든의 기존 글들과 일관).

```
---
title: "제목 (저자) — 핵심 통찰 한 줄"
source_title, source_url, source_name, referrer_url(=GeekNews 링크),
published_at, summarized_at, category, tags: [...]
---
# 제목
> 출처: [원문 제목](url) (저자 · 경로) · 정리일 YYYY-MM-DD
## 한 줄 요약        (굵게, 논지 압축)
## 핵심 포인트        (불릿, ***핵심구절*** 강조)
## 인상 깊은 문장      (원문/요약 인용)
## 댓글              (hada 댓글 수 + HN/Lobsters 큐레이션 유무 + 출처 한계·편향 정직하게)
## 내 생각 · 적용점     (핵심 전이 N개, [[전체파일명]] 위키링크로 다른 노트와 연결)
## 호스피탈리티 / CRS 적용 포인트   (온다=B2B 호스피탈리티/CRS 관점, 직접 적용 없으면 정직하게 밝힘)
## 연관 자료          ([[전체파일명]] — 관계 한 줄)
## 한 달 뒤 회고        (YYYY-MM-DD 즈음 점검 항목)
```

## 위키링크 규약

- **노트 본문**: 전체 파일명 `[[2026-07-18-slug]]` (확장자 없이). 커밋 전 실재 확인.
- **README 인덱스**: 짧은 한글 별칭 `[[취향경제]]` `[[Wes매키니]]` 등. 기존 별칭을 재사용하고, 없으면 새로 짓는다(dangling 링크는 미래 연결 표시로 허용).

## 품질 기준

- **정직성**: 댓글 수·n=1·아젠다·이해관계·봇 차단 등 한계를 요약에 명시. "PASS"만 보고 금지.
- **핵심 전이**: 요약에 그치지 말고 다른 노트와의 연결(계열·반증·거울상)을 세운다. 억지 연결은 금물 — 리소스·유머 글은 가볍게, 진짜 연결되는 축만.
- **CRS 접점**: 온다(호스피탈리티/CRS, B2B) 관점 적용을 시도하되, 억지면 "직접 적용은 멀다"고 밝히고 전이 가능한 원칙만 남긴다.
